The RunClassificationDataGenerator is a class that generates data for model testing and is therefore not being tested
in the test suite.

During model training, the training is being test against a set of the learning data the model has not seen before which
has achieved an accuracy of 99%

Additionally, the model derived from it has been throughoutly tested with real world data, for a better real world
comparison.